
<% if (@post.comments.count > 0) %>
	<% @post.comments.each do |c| %>
	  <hr>
	  <p>
	    <%= link_to User.find(c.user_id).username, user_path(User.friendly.find(c.user_id))%> : <%= c.text %></p>
	<% end %>
<% else %>
	This post has no comments yet.
<% end %>